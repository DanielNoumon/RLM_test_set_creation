{
  "name": "test-set-viewer",
  "version": "1.0.0",
  "description": "Simple viewer for RLM test set JSON files",
  "scripts": {
    "build": "tsc",
    "start": "tsc && lite-server",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "lite-server": "^2.6.1"
  }
}
